import EventOpenGraphImage, { alt, contentType, size } from "@/app/events/[id]/opengraph-image";

export { alt, contentType, size };
export const runtime = "edge";

export default EventOpenGraphImage;
